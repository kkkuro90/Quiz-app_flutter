name: quiz_app
description: A Flutter quiz application for educational purposes

publish_to: 'none'

version: 1.0.0+1

environment:
  sdk: '>=3.0.0 <4.0.0'

dependencies:
  flutter:
    sdk: flutter
  provider: ^6.1.1
  # Для следующих этапов:
  # firebase_core: ^2.24.0
  # firebase_auth: ^4.17.2
  # cloud_firestore: ^4.13.0
  # http: ^1.1.0
  # file_picker: ^5.5.0

dev_dependencies:
  flutter_test:
    sdk: flutter
  flutter_lints: ^3.0.0

flutter:
  uses-material-design: true
  assets:
    - assets/images/